/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.dummyjson.com', 'dummyjson.com', 'robohash.org', 'i.dummyjson.com'],
  },
};

module.exports = nextConfig;
